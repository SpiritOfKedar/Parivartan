# Conversion engine (Node.js — no LibreOffice required)
CONVERSION_ENGINE=node

# Chromium for Word → PDF (set automatically in Docker)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# OCR languages for scanned PDFs (tesseract.js)
OCR_LANGUAGES=eng
